@model IEnumerable<MVCTravelBookingISE.Models.BookingModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Booking_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Flight_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Acco_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Traveller_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Booking_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Booking_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flight_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Acco_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Traveller_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Booking_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Booking_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Booking_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
