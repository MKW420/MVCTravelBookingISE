@model IEnumerable<MVCTravelBookingISE.Models.FlightRulesModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Flight_Descrip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Flight_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Flight_Descrip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Flight_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FlightRules_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FlightRules_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FlightRules_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
